# ns-gm credential configuration is profile-based.
# Run `ns-gm setup` to create/select profiles stored at:
#   ~/.ns-gm/credentials.json
#
# This file is intentionally minimal and reserved for optional future
# process-level settings.
